import React from "react";
import styles from "./Bottom.module.css";

function Bottom() {
  return (
    <div className={`${styles.bottombar} ${styles.gridlayout}`}>Bottom</div>
  );
}

export default Bottom;
